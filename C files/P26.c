#include <stdio.h>
#include <stdlib.h>

struct node
{
    int data;
    struct node *link;
};

void enqueue(struct node *f,struct node *r,int val){
    struct node *n = (struct node *)malloc(sizeof(struct node));
    
}












int main(){
    struct node *f = NULL;
    struct node *r = NULL;
    return 0;
}